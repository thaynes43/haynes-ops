apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns-cloudflare
spec:
  interval: 30m
  chart:
    spec:
      # https://github.com/kubernetes-sigs/external-dns/blob/master/charts/external-dns/values.yaml
      chart: external-dns
      version: 1.15.0
      sourceRef:
        kind: HelmRepository
        name: external-dns
        namespace: flux-system
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    crds: CreateReplace
    remediation:
      strategy: rollback
      retries: 3
  values:
    provider:
      name: cloudflare
    env:
      - name: CF_API_TOKEN
        valueFrom:
          secretKeyRef:
            name: external-dns-cloudflare-secret
            key: cloudflare_api_token
    extraArgs:
      - --cloudflare-proxied
      - --traefik-disable-legacy
      - --ingress-class=traefik-external # TODO this did not work for UniFi, likely only the domain filter is controlling what this adds entries for
    logLevel: debug
    policy: sync
    sources: ["crd", "ingress", "traefik-proxy"]
    domainFilters: ["hayneshosted.com"]
    excludeDomains: ["haynesnetwork.com", "haynesops.com", "hayneslab.net"] # TODO as of now these are only controlled by the other cluster