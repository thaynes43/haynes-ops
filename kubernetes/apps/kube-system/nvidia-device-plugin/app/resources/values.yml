---
version: v1
flags:
  migStrategy: single
  plugin:
    deviceListStrategy: envvar
    deviceIDStrategy: uuid
    securityContext:
      privileged: true
sharing:
  timeSlicing:
    resources:
      - name: nvidia.com/gpu
        replicas: 3
tolerations: []