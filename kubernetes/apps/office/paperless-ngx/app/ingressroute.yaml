apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: paperless-ngx
  annotations: # TODO external-dns does not work with the middleware!
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`paperless.haynesnetwork.com`) && PathPrefix(`/`)
      services:
        - kind: Service
          name: paperless-ngx
          port: 80
      middlewares:
        - name: ak-outpost-authentik-embedded-outpost
          namespace: network
  tls:
    secretName: certificate-haynesnetwork