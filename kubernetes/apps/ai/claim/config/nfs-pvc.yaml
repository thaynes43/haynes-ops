apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-hdd-nfs-pvc-ai
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Ti
  volumeName: cephfs-hdd-nfs-pv-ai
  storageClassName: ""