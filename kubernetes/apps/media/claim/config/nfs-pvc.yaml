apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-hdd-nfs-pvc-media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Ti
  volumeName: cephfs-hdd-nfs-pv-media