apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reflector
spec:
  chart:
    spec:
      chart: reflector
      version: 10.0.x
      sourceRef:
        kind: HelmRepository
        name: emberstack
        namespace: kube-system
  interval: 15m
  timeout: 5m
  releaseName: reflector