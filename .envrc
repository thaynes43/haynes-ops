#shellcheck disable=SC2148,SC2155
export KUBECONFIG="$(expand_path ./kubeconfig)"
export SOPS_AGE_KEY_FILE="$(expand_path ./age.key)"
# Talos
export TALOSCONFIG="$(expand_path ./kubernetes/main/bootstrap/talos/haynes-ops-talosconfig.yaml)"
export OMNICONFIG="$(expand_path ./kubernetes/main/bootstrap/omni/haynes-ops-omniconfig.yaml)"
# Bin
PATH_add "$(expand_path ./.bin)"