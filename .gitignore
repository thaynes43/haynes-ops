# Trash
.DS_Store
Thumbs.db
# k8s
.decrypted~*.yaml
.config.env
*.agekey
*.pub
*.key
# Private
.private
.bin
# Ansible
.venv*
# Taskfile
.task
# Brew
Brewfile.lock.json
# intellij
.idea
# wiki
wiki
# Bootstrap
/config.yaml
# Cloudflare Tunnel credentials (generated locally)
/cloudflare-tunnel.json
# Uncomment configs if not using auth w/ omni 
#kubeconfig
#talosconfig
#omniconfig
.env
zprompt.md
# Claude Code local working dir + session/handoff scratch (kept local, not repo content)
# — but DO track shared project slash commands under .claude/commands/.
.claude/*
!.claude/commands/
HANDOFF-*.md
SESSION-HANDOFF.md